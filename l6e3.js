//- Criar um algoritmo que imprima a soma dos números pares entre 25 e 200. 

/*
var soma, cont;

soma = 0;
cont = 25;

while (cont <= 200)
{
	if (cont % 2 == 0)
		soma += cont;
	cont++;
}
document.write("Resultado = " + soma);
*/
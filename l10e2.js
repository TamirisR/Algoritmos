// Solicite 5 números. Exiba os que estão acima da média.

/*
var numero = [], media, soma, cont;

soma = 0;
for (cont=0; cont < 5; cont++)
{
  numero[cont] = parseInt(prompt("digite um numero"));
  soma += numero[cont];
}
media = soma / 5;

document.write("A média dos 5 números foi: " + media + "<br>");.

for (cont=0; cont < 5; cont++)
{
  if (numero[cont] > media )
  	document.write(numero[cont] + " é maior que a média" + "<br>");
}
*/
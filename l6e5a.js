// a - Escreva um programa que solicita o nome e a altura de 5 atletas. Ao final da entrada de dados, informe O NOME do atleta mais alto. 

/*
	var atleta, cont, nome, altura, maiorAltura, nomeDoMaior;

	cont = 1;
	maiorAltura = 0;
	nomeDoMaior = "";

	while (cont <= 5)	
	{
		nome = prompt("Digite o nome do atleta: ");
		altura = parseInt(prompt("Digite a altura: "));

		if (altura > maiorAltura)
		{
			maiorAltura = altura;
			nomeDoMaior = nome;
		}
		cont++;
	}
	document.write("A maior altura foi : " + maiorAltura + " cm, do atleta: " + nomeDoMaior);
*/

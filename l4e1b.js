// Escreva um programa que sorteia um número de 1 a 10, dá duas chances para que o usuário acerte, mas informa se o número sorteado é maior ou menor que o primeiro palpite.

/*
var	numUsuario, numSorteio;

numUsuario = parseInt(prompt("Digite um número: "));
numSorteio = Math.floor(Math.random() * 100) % 10 +1;

if (numUsuario == numSorteio){
  document.write("Você acertou!!");
} 
else { 
  if (numUsuario < numSorteio){
		numUsuario = parseInt(prompt("errou, é um número maior, tente novamente"));
	}
	else {
		numUsuario = parseInt(prompt("errou, é um número menor, tente novamente"));
	}
	if (numSorteio == numUsuario)
			document.write("voce acertou");
	else
			document.write("Acabou suas chance, o numero sorteado foi: " + numSorteio);
}
*/


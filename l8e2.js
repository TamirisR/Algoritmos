//  Escreva uma função chamada ParOuImpar, que recebe um número inteiro e retorna um texto com a palavra "PAR" ou "IMPAR".
/*
var n

n = parseInt(prompt("digite um numero"));
document.write("O número é: " + ParOuImpar(n));

function ParOuImpar(x)
{
	resultado = "";

	if (x % 2 == 0)
		resultado = "Par";
	else 
		resultado = "Impar";
	return (resultado);	
}

document.write("<br>");

var num;

num = parseInt(prompt("Digite um número"));
document.write(parOuImpar(num));

function parOuImpar(valor)
{
	var mensagem;
	if (valor % 2 == 0)
		mensagem = "par";
	else
		mensagem = "impar";

	return mensagem;

}
*/
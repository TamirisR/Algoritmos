// Leia uma frase. Exiba quantas letras "a" esta frase possui.

/*
var	frase, letra, qtdeLetra;

frase = prompt("Digite uma frase: ");
letra = prompt("Digite a letra a ser contada: ");

qtdeLetra = contadorDeLetras(frase, letra);
document.write("A frase: '" + frase + "' tem: " + qtdeLetra + " '" + letra + "'");

function contadorDeLetras(frase, letra)
{
	var i, tamDaFrase, contDeLetra;
	tamDaFrase = frase.length;

	contDeLetra = 0;
	for (i = 0; i < tamDaFrase; i++)
	{
		if (frase.charAt(i) == 'a')
			contDeLetra++;
	}
	return (contDeLetra);
}
*/





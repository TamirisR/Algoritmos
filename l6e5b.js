// b informe também a altura média destes 5 atletas. 
/*
	var atleta, cont, nome, altura, maiorAltura, nomeDoMaior, somaAltura, media;

	cont = 1;
	maiorAltura = 0;
	nomeDoMaior = "";
	somaAltura = 0;

	while (cont <= 5)	
	{
		nome = prompt("Digite o nome do atleta: ");
		altura = parseInt(prompt("Digite a altura: "));
		somaAltura += altura;
		if (altura > maiorAltura)
		{
			maiorAltura = altura;
			nomeDoMaior = nome;
		}
		cont++;
	}
	media = somaAltura / 5;
	document.write("A maior altura foi : " + maiorAltura + " cm, do atleta: " + nomeDoMaior + " e a média de altura foi de: " + media + " cm");

*/
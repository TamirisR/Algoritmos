// Escreva uma função chamada EhPrimo, que recebe um número inteiro e retorna um texto dizendo se este é ou não um número primo.

/*
//Input
var numero;
numero = parseInt(prompt("Digite um número para saber se ele e primo"));
document.write(EhPrimo(numero));

//Função
function EhPrimo(num)
{
	var	mensagem, cont, numeroDivisores;

	cont = 1;
	numeroDivisores = 0;
	while (cont <= num)
	{       
		if (num % cont == 0)	
			numeroDivisores++; 
		cont++;	
	}

	if (numeroDivisores == 2)
		mensagem = "Este é um número primo.";
	else 
		mensagem = "Este não é um número primo.";
	return mensagem;
}
*/
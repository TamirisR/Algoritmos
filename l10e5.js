// Escreva o conteúdo do vetor do exercício anterior na tela, colocando cada valor ao lado do anterior (na mesma linha). Se o valor do vetor for múltiplo de 10, então insira uma quebra de linha.

/*
var nAleatorios, cont, soma;

nAleatorios = [];
soma = 0;
for (cont = 0; cont < 100; cont++)
{ 
	nAleatorios[cont] = Math.floor(Math.random() * 100);
	if (cont >= 89)	
		soma += nAleatorios[cont];
}
media = soma / 10;
document.write("A média dos 10 últimos números aleatórios é: " + media + "<br>");

for ( cont= 0; cont <= 99; cont++ )
{
  if (nAleatorios[cont] < media)
	{
		if (nAleatorios[cont] % 10 == 0)
  		document.write(nAleatorios[cont] + "<br>");
		else 
			document.write(nAleatorios[cont] + " ");
	}
}
*/

// Escreva uma função que possui 2 parâmetros, um valor de temperatura, e um caracter que será ‘C’ ou ‘F’, e fará a conversão da temperatura informada de Celsius para Fahrenheit, ou vice-versa, conforme o segundo argumento.

/*
//C --> Fahrenheit para Celsius
//F --> Celsius para Fahrenheit
document.write(conversorDeTemperatura(25, 'F') + "<br>");
document.write(conversorDeTemperatura(50, 'C') + "<br>");

function conversorDeTemperatura(temperatura, conversao)
{
	var resultado

		if (conversao == 'C') // Fahrenheit para Celsius
			resultado = (((temperatura - 32) * 5) / 9); 
		else								 // Celsius para Fahrenheit
			resultado = 1.8 * temperatura + 32;
		return (resultado);
}
*/
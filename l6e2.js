//Adivinhe o número - Crie um programa que solicita um número ao usuário entre 1 e 10, sorteia um outro número dentro desta faixa de valores, e então diz se os dois números são iguais.

/*
var num = 0, numSorteio;

while (num < 1 || num > 10)
	num = parseInt(prompt("Digite um número entre 1 e 10"));

numSorteio = Math.floor(Math.random() * 100) % 10 +1;
if (num == numSorteio){
  document.write("são iguais, o número sorteado foi: " + numSorteio);
}
else {
  document.write("não são iguais, o número sorteado foi: " + numSorteio);
}
*/
